ARG REGISTRY_URL

FROM ${REGISTRY_URL}/debian:bullseye

RUN <<EOF

echo 'deb [trusted=yes] https://repo.goreleaser.com/apt/ /' | tee /etc/apt/sources.list.d/goreleaser.list

apt-get update
apt-get install -y debmake pbuilder aptitude zstd nfpm

apt-get clean

EOF

WORKDIR /src
